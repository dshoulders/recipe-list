{
  "scripts": {
    "api": "node api.js",
    "build": "webpack -p --config webpack.production.config.js",
    "deploy": "npm run build & node deploy.js & firebase deploy",
    "client": "webpack-dev-server --port 3000"
  },
  "dependencies": {
    "babel-preset-es2016": "^6.22.0",
    "hyperapp": "^0.7.1",
    "json-server": "0.9.5"
  },
  "devDependencies": {
    "ramda": "^0.23.0",
    "webpack": "^2.3.2",
    "webpack-dev-server": "^2.4.2"
  }
}
